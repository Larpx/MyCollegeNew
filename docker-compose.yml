# ============================================================
# Campus Attendance docker-compose 编排文件
# 四个服务：redis（缓存）、db（MySQL）、api（后端 API）、web（Blazor Server 前端）
# 启动顺序：db(healthy) + redis(healthy) → api(healthy) → web
# ============================================================

services:
  # ---- Redis 缓存 ----
  redis:
    image: redis:7-alpine
    container_name: attendance-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ---- MySQL 数据库 ----
  db:
    image: mysql:8.0
    container_name: attendance-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: attendance
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # ---- 后端 API ----
  api:
    build:
      context: ./src
      dockerfile: Campus.Attendance.Api/Dockerfile
    container_name: attendance-api
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - Db__ProviderType=MySQL
      - Db__ConnectionString=Server=db;Port=3306;Database=attendance;Uid=root;Pwd=root;
      - Jwt__SecretKey=${JWT_SECRET_KEY:-Campus.Attendance.SecretKey.2026.DoNotHardCode.InProduction}
      - Jwt__Issuer=Campus.Attendance.Api
      - Jwt__Audience=Campus.Attendance.Client
      - Jwt__ExpireMinutes=120
      - ConnectionStrings__redis=redis:6379
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://aspire-dashboard:18889
    ports:
      - "5000:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -s -o /dev/null http://localhost:8080/ || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 40s
    restart: unless-stopped

  # ---- Blazor Server 前端 ----
  web:
    build:
      context: ./src
      dockerfile: Campus.Attendance.Web/Dockerfile
    container_name: attendance-web
    depends_on:
      api:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - Api__BaseUrl=http://api:8080
      - Jwt__SecretKey=${JWT_SECRET_KEY:-Campus.Attendance.SecretKey.2026.DoNotHardCode.InProduction}
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/ || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 40s
    restart: unless-stopped

volumes:
  mysql_data:
